#pragma once

namespace alCameraBlurFunction {

setCameraBlurName(al::LiveActor const*, char const*);
resetCameraBlurName(al::LiveActor const*);
invalidateCameraBlur(al::LiveActor const*);
validateCameraBlur(al::LiveActor const*);

} 
