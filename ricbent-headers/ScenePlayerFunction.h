#pragma once

namespace ScenePlayerFunction {

forceShowPlayer(al::Scene const*);
forceHidePlayer(al::Scene const*);
startSnapShotMode(al::Scene const*);
endSnapShotMode(al::Scene const*);
updatePlayerDither(al::Scene const*);

} 
